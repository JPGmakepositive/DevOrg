<apex:page controller="aggregateClass">
    <apex:pageBlock >
        <apex:pageBlockTable value="{!results}" var="r">
            
            <apex:column value="{!r['e']}"/>
            <apex:column value="{!r['h']}"/>
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>